using UnityEngine;
using System.Collections;

/*public class EMField : MonoBehaviour {
	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        
	}
    void applyForces(Vector3 center, float radius)
    {
        Collider[] EMColliders = Physics.OverlapSphere(center, radius);
        Vector3 pos1;
        Vector3 pos2;
        float charge1;
        float charge2;
        Vector3 E;
        for (int i = 0; i < EMColliders.Length; i++)
        {
            pos1 = EMColliders[i].transform.position;
            charge1 = EMColliders[i].
        }
    }


}
*/